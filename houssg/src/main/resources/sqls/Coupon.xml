<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ssg.com.houssg.dao.CouponDao">

	<!-- 쿠폰 발행  -->
	<insert id="couponIssued" parameterType="ssg.com.houssg.dto.CouponDto">
		insert into coupon(coupon_number, coupon_name, discount, expiration_date)
		values (#{couponNumber}, #{couponName}, #{discount}, #{expirationDate})
	</insert>
	
	<!-- 쿠폰번호 중복확인 -->
	<select id="couponNumberCheck" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(*)
		from coupon
		where coupon_number=#{conponNumber}
	</select>
	
</mapper>